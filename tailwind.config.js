export default {
    content: ['./src/**/*.{html,js,svelte,ts}'],
    darkMode: 'class', // 'media' para system preference, 'class' para control manual
    theme: {
        extend: {
            // Tus extensiones de tema aquí
        },
    },
    plugins: [],
}